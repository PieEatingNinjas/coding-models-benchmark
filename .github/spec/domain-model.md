---
title: Domain Model
category: data-model
priority: 1
tags: [dto, secret, efcore, tags]
source: [src/TodoApi/Models]
related_docs: [api-contracts.md]
summary: TodoItem entity, TodoItemDto, and the mapping/Secret rule.
---

# Domain Model

## TodoItem (entity — `src/TodoApi.Domain/Models/TodoItem.cs`)
| Field | Type | Note |
|-------|------|------|
| `Id` | `int` | PK, generated by EF |
| `Name` | `string?` | task title |
| `IsComplete` | `bool` | done yes/no |
| `Priority` | `TodoPriority` | `Low`/`Medium`/`High`, default `Medium` |
| `DueDate` | `DateTimeOffset?` | optional timezone-aware deadline |
| `Tags` | `List<string>` | normalized, trimmed, lowercase tags; empty means no tags |
| `Secret` | `string?` | **internal; must never go out via the API** |

## TodoItemDto (contract — `src/TodoApi.Domain/Models/TodoItemDto.cs`)
Contains `Id`, `Name`, `IsComplete`, `Priority`, `DueDate`, and `Tags`. Has **no** `Secret` property — a compile-time guarantee that the field cannot leak. `Tags` is always a collection, never `null`; an item without tags returns an empty collection.

## Mapping
`TodoItemMapper.ToDto(this TodoItem)` in the Application layer projects entity → DTO. The API endpoints delegate to `TodoItemService`, which performs the mapping and ensures the DTO never exposes `Secret`. Tag values are normalized to lowercase, trimmed, and deduplicated before being exposed in the DTO.

## Persistence
`TodoDb : DbContext` in `src/TodoApi.Infrastructure/Data` has `DbSet<TodoItem> Todos`, registered with the InMemory provider (`UseInMemoryDatabase("TodoList")`). The `Tags` collection is persisted as a JSON-serialized string value so the InMemory provider can store it. Integration tests replace this with a uniquely named InMemory store per test class.

## Invariants
- A DTO never contains sensitive fields.
- `Id` is assigned by persistence, not by the client (a POST-body `Id` is ignored).
- Tags are normalized to lowercase, trimmed, and deduplicated so the set semantics are preserved.

## Layering
- `src/TodoApi.Domain` owns the Todo entities and value objects.
- `src/TodoApi.Application` contains use cases/services and repository abstractions.
- `src/TodoApi.Infrastructure` implements the repository and EF Core persistence.
- `src/TodoApi` contains the Minimal API endpoints and DI wiring.
